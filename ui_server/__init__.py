"""UI server package for agent-context-local web dashboard.

Provides a FastAPI REST API layer that wraps the existing CodeSearchServer
business logic, and serves the compiled React frontend as static files.

Entry point: ``agent-context-local-ui``  (wired in pyproject.toml)
"""
